# CLAUDE.md

## Agent skills

### Issue tracker

Issues are tracked in Trello (Backlog → In Progress → Done). See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical labels apply (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo: one `CONTEXT.md` + `docs/adr/` at the root. See `docs/agents/domain.md`.
